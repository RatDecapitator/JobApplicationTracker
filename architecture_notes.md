# Architecture Notes

## Application IDs

Decision:
- JobApplication contains an `id` field.
- IDs are assigned by JobApplicationManager.
- In future, IDs will be generated by the database.

Reason:
- Keeps domain objects simple.
- Easier migration to SQLite/PostgreSQL.
- Aligns with REST API design.